import Auth from './Auth';

class Name extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            loading: true
        };
    };
}

export default Name;
