doctype html
html(lang="ja")
   head
      meta(charset='utf-8')
      meta(name="viewport", content="width=device-width, initial-scale=1")
      meta(name="description", content="ここに説明を挿入")
      link(href="https://fonts.googleapis.com/css?family=Lato:400,700|Noto+Sans+JP:400,700", rel="stylesheet")
      link(href="/styles.css", rel="stylesheet")
      title= title
      script(src="https://code.jquery.com/jquery-3.5.1.min.js", integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0=", crossorigin="anonymous")
      script(src="https://www.gstatic.com/firebasejs/7.9.3/firebase-app.js")
      script(src="https://www.gstatic.com/firebasejs/7.9.3/firebase-auth.js")
      script(src="https://www.gstatic.com/firebasejs/7.9.3/firebase-database.js")
      script(src="https://www.gstatic.com/firebasejs/7.9.3/firebase-firestore.js")
      script(src="/signin.js")
      block header
   body
      header.symple-header
         div.header-content
            | 東京寮ウェブアプリ
      main
         div.maincontent
            div.box
               h2 ログイン
               span
                  | アカウントを作成済みの寮生はここからログインしてください。
               form.lemono-auth-signin(name="sign-in")
                  span.fortextbox メールアドレス
                  input.normaltextbox(type="text" name="email")
                  span.fortextbox パスワード
                  input.normaltextbox(type="password" name="password")
                  button.submitbutton ログイン
            div.box
               h2 アカウントを作成
               | アカウントをまだ作成していない寮生はここから作成してください。<br>
               | 寮生共通パスワードとは、旧タイプの寮生専用サイトと同じものです。<br>
               | 分からない場合はネットワーク委員にきいて下さい。
               form.auth-confirm(name="confirm", action="./confirm", method="post")
                  span.fortextbox 寮生共通パスワード
                  input.normaltextbox(type="password" name="password")
                  button.submitbutton アカウントを作成
